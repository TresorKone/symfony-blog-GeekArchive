{{ form_start(form, {'attr': {'novalidate': 'novalidate'}}) }}
    <div>{{ form_errors(form) }}</div>

    <div class="block mb-2 text-sm font-medium text-gray-900 dark:text-gray-300">
        {{ form_label(form.name) }}
    </div>
    <div class="">
        {{ form_widget(form.name, {'attr': {'class': 'mb-6 bg-gray-50 border border-gray-300 text-gray-900 text-sm rounded-lg focus:ring-blue-500 focus:border-blue-500 block w-full p-2.5 dark:bg-gray-700 dark:border-gray-600 dark:placeholder-gray-400 dark:text-white dark:focus:ring-blue-500 dark:focus:border-blue-500'}}) }}
        {{ form_errors(form.name) }}
    </div>

    <div class="block mb-2 text-sm font-medium text-gray-900 dark:text-gray-300">
        {{ form_label(form.description) }}
    </div>
    <div class="">
        {{ form_widget(form.description, {'attr': {'class': 'mb-6 bg-gray-50 border border-gray-300 text-gray-900 text-sm rounded-lg focus:ring-blue-500 focus:border-blue-500 block w-full p-2.5 dark:bg-gray-700 dark:border-gray-600 dark:placeholder-gray-400 dark:text-white dark:focus:ring-blue-500 dark:focus:border-blue-500'}}) }}
        {{ form_errors(form.description) }}
    </div>

    <button class="btn">{{ button_label|default('Save') }}</button>
{{ form_end(form) }}
